export default class CampoRegistro {
  constructor ({ id, inicio, fim, valor, tipo, label, disabled }) {
    this.id = id
    this.inicio = inicio
    this.fim = fim
    this.valor = valor
    this.tipo = tipo
    this.label = label
    this.disabled = disabled
  }
}
